# GitHub 发布网页仓库 · 小白速成指南

> 目标：把你自己写的网页（一个 `index.html` 文件）发布到 GitHub 上，得到一个能直接点开访问的链接。
> 全程 10 分钟，不需要懂代码。

---

## 需要准备什么

1. 一个 GitHub 账号（没有的话先去 [github.com](https://github.com) 免费注册一个）
2. 一个网页文件，文件名必须叫 **`index.html`**（这是网页的"主文件"，浏览器认这个名字）

> 没有网页文件？用记事本新建一个文件，输入下面内容，另存为 `index.html` 即可：
>
> ```html
> <h1>你好，这是我的第一个网页！</h1>
> ```

---

## 第 1 步：创建仓库

仓库（Repository）可以理解为 GitHub 上的一个文件夹，用来放你的文件。

1. 登录 GitHub，点右上角的 **`+`** 号 → **New repository**
2. 在 **Repository name** 里填仓库名，只能用英文（例如 `mypage`）
3. **必须选 Public**（公开），否则免费方案无法发布网页
4. 可以勾选 **Add a README file**（自动生成一个说明文件）
5. 点绿色的 **Create repository** 完成创建

---

## 第 2 步：上传网页文件

把 `index.html` 传到刚才的仓库里：

1. 进入你的仓库页面，点 **Add file** → **Upload files**
2. 把你的 `index.html` 文件拖进页面中间的虚线框
3. 点绿色的 **Commit changes**（提交，第一次保持默认即可）

此时文件已经上传成功，但**还不能访问**，还需要最后一步"发布"。

---

## 第 3 步：开启 GitHub Pages（发布）

GitHub Pages 是 GitHub 自带的免费网页托管服务，开启后才会生成访问链接。

1. 在仓库页面点顶部菜单的 **Settings**（设置）
2. 点左侧边栏的 **Pages**
3. 找到 **Build and deployment**（构建和部署）区域，这里有三个地方要操作：

   **第一个：Source（来源）**
   点右边的下拉框，选择 **Deploy from a branch**（从分支部署）。

   **第二个：Branch（分支）**
   这一行有两个下拉框，挨在一起：
   - 左边的下拉框：点开，选 **main**
   - 右边的下拉框：点开，选 **/ (root)**（斜杠加 root，意思是仓库最外层目录）

   **第三个：Save（保存）**
   选完后点右侧的 **Save** 按钮保存。

4. 保存成功后，页面上会显示一句提示：*Your GitHub Pages site is currently being built from the main branch*（正在从 main 分支构建你的网站），看到这句就说明设置生效了，可以放心关掉页面。

之后等 **1～5 分钟**，GitHub 会自动完成构建和部署，构建完成后这句提示会变成你的网站链接。

---

## 访问你的网页

部署完成后，你的网页链接是：

```
https://你的用户名.github.io/仓库名/
```

举例：用户名是 `chanvvgeo`，仓库名是 `chanvvgeo`，链接就是：

**https://chanvvgeo.github.io/chanvvgeo/**

把链接发到浏览器打开，能看到页面内容，就大功告成了！

---

## 常见问题（FAQ）

**Q：打开链接显示 404（打不开）？**
A：最常见原因是还在构建中，等 1～5 分钟再刷新。如果一直 404，检查：① Pages 设置是否点过 Save；② 文件名是不是 `index.html`（大小写都要对）；③ 链接里的用户名和仓库名是否与你的账号完全一致。

**Q：网页内容更新了，怎么生效？**
A：重新进入仓库，用"上传文件"的方式**覆盖** `index.html`（或点文件旁的铅笔图标直接编辑），提交后等 1～2 分钟，刷新页面就是新版。

**Q：为什么必须是 `index.html`？**
A：这是网页服务器的约定——`index.html` 是网站的默认首页，访问链接时浏览器会自动找这个名字的文件。

**Q：仓库是私有的（Private）可以吗？**
A：免费版 GitHub 的 Pages 只支持公开仓库。想用私有仓库发布网页，需要付费的 GitHub Pro。

---

*说明：本文以 GitHub 网页版操作为例，不需要安装任何软件。*
